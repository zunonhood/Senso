# secrets — never commit the real AI key
bot/ai.config.json

# node
node_modules/
npm-debug.log*

# bot runtime state (regenerated as it builds)
bot/state.json
bot/nyc-progress.json

# os / editor
Thumbs.db
.DS_Store
*.log
